# 能力实验记录（2026-09-10）

2026-09-10。对统一 display 回调后的实现进行测试。模型为本地配置的 `deepseek-v4-flash`，隐藏 thinking 关闭。业务核心、提示词与工具规则未为实验改动。实验脚本复用生产 Agent，参数与输出见脚本。

## 执行范围与判定

|问题|原来缺什么|本轮做了什么|当前结论|
|生成中 Ctrl+C|真实输出尚未完成时的取消与恢复|实际 PTY 发送 Ctrl+C，取消输入修改暗号，随后追问旧暗号|一次有效取消恢复通过；工具阻塞阶段、物理键盘及资源释放测量未覆盖|
|CLI 包装真实回归|此前已有工程/安装验证，缺真实服务链路|安装入口真实聊天、取消后追问、搜索；独立目录显式配置及连续问答|实际流程可运行，独立子进程退出码 0；搜索答案的版本选择另有问题|
|指定页读取|定位 mimalloc 小节、续读和范围表述|真实 page 用例，保存各段正文及请求|3 轮、两段 0–24000/24000–35449，获取能力通过；回答额外扩展，不能认证每句正确|
|结构化 think|不清楚结构是否改善证据缺口判断和下一步|同一真实会话与页面快照，schema 是唯一首请求差异，交错跑自由/结构化/结构化/自由|四次都识别缺口并续读；没有观察到结构化的额外收益，发现操作引用误填和两次无效调用|
|复杂 mimalloc 问题|版本归因、构建支持与运行时默认、读取覆盖的具体错误|原 boundary 问题原样复测，完整保存实际取得证据|9 轮、36.123 秒，仍有证据范围扩大与归因问题，部分通过|
|Extract 目标不匹配|缺失路径返回入门页，不知哪一层造成|本机 HTTP 与 Tavily basic，正常页/两个缺失路径/首页对照，缺失路径各两次提取|本机直接访问也返回首页内容；站点访问层回退是可观察因素，不必假设 Tavily 凭空替换页面|

本轮没有评估 advanced 收益、噪声过滤、长期成功率、无 think 对照或自主选择结构化笔记的通用收益。

## T1：think 格式对照

先真实读取 memory 页首段并正常结束一次问答。保留完整 Session 和页面快照，首段 0–24000，快照共 37718 字符；目标标题正文位于字符 32264 处，确实不在首段。四组从该 Session 的独立深复制开始，追问原文保存在记录中，明确要求先调用 think，再决定是否续读。

首请求除了 think 工具的 parameters schema 外逐字段相等，断言及公共请求哈希已记录。后续分叉允许自然发生；读取 start=24000 使用同一快照，不重新联网。只保存显式笔记/正文和工具结果，不保存隐藏 reasoning_content。

|组|模型轮次|think 调用/失败|后续读取|最终判断与观察|
|---|---:|---:|---|---|
|自由 1|3|1/0|24000–37718|指出页面未记载，不推断现实中不存在；准确列出两段范围|
|结构化 1|5|3/2|24000–37718|先错引 snapshot_id，再错引工具名，最后空 references 成功；结论区分未记载，但将提取快照覆盖描述为整个页面，范围措辞仍不严谨|
|结构化 2|3|1/0|24000–37718|references 为空；核心小节判断正确，但出现“不属于未记载，是页面中不存在该规定”的概念表述偏离要求。未断言现实中无要求，不能夸大成现实事实反转|
|自由 2|3|1/0|24000–37718|区分页面未记载与现实不存在；将本地 partial 称作 provider 标注，状态来源归因不准确|

四组笔记都看到了缺口并提出续读，但无法据此断定 think 导致了该行为，因为没有无 think 对照。自由笔记中也有“出现一次/三次”等计数不准确；schema 合法不保证笔记事实正确。

操作引用失败有确切对象：正确 tool_call_id 是 `call_00_VD0Ot7Pmp8h6LSom2ait9349`，模型首先填入的 `b76485fd1b4dad30` 实际是 snapshot_id，第二次填 `read_page`。这两次错误是模型参数选择失败，校验按当前规则正确拒绝；尚不能确定是 ID 可见性、字段说明、长上下文定位还是模型自身造成。下一轮可固定失败前请求，只改变引用定位信息做对照；

允许结论：本题强制整理场景中，结构化形式没有显示出相对自由笔记的收益，并可能引入引用修复成本。样本各两次，不能推广为结构化一定无用。

证据：[完整对照](think-pair-20260910.json)，脚本：[运行器](../../scripts/probe_capabilities_20260910.py)。

## F1：复杂题必须保留的具体问题

问题原文：根据 Python 3.13 官方文档，mimalloc 在普通 GIL 构建与 free-threaded 构建中的启用条件分别是什么？捆绑、默认使用和必须使用是否相同？请提供官方来源，不要混用其他 Python 版本。

本轮实际做了两次搜索、五次 read_page、两次自由 think。memory 快照完整两段读取；free-threading 页读取 10012 字符；configure 只读 0–48000 / 56562，尚有 8562 字符未返回。

已改善/做到：模型识别搜索里有 3.14 片段，主动读取 3.13 页面；最终把构建可用与运行时默认区分开，没有简单以“enabled by default”直接证明运行时默认使用。

仍存在的问题：

1. **从局部证据扩大为整个文档集合的否定。** 答案多次说“3.13 官方文档并未写出……必须使用”，但只核查了少数页面，configure 甚至未读到快照末尾。这些证据不能证明整个版本文档中不存在该表述。无需先判定真实 mimalloc 行为，也能判定这个归纳超出了已获取证据。
2. **引用内容与支持主张不完全对应。** 开头用 configure 的 “Disable … enabled by default” 支持“随源码捆绑”；这条引文本身只说明开关和默认支持状态。捆绑应对应实际搜索中取得的 What's New 相关说明，不能把相邻事实都归给同一引文。
3. **把表格范围误说成环境变量全部取值。** 答案声称 `PYTHONMALLOC` 取值“只有 pymalloc/malloc 系”，但工具正文图例同时列出 mimalloc。至少存在内部证据解释不一致；不据此延伸断言所有运行时行为。

下一次有效对照应针对上述一种错误：固定真实请求和已取得证据，检验模型能否约束主张范围，或补入精确版本来源后是否纠正。不要以“再问一次复杂题正常结束”当作修复，也不要只用新提示词同时改变多个因素。

证据：[boundary 原始请求、工具正文与答案](boundary-20260910.json)、[指定页用例](page-20260910.json)。

## E1：Extract 成功但请求目标不匹配

原路径 `/mini-core-nonexistent-20260908`，新增不同路径 `/seekra-nonexistent-20260910`；正常对照为 `/zh-cn/guides/vision`，另读取站点首页 `/`。

- 本机 HTTP：正常 Vision 页 302 到带尾斜杠地址，最终 200、标题为图像理解。两个缺失路径均直接 200、无 HTTP 跳转，标题为 Your First API Call；两者与首页的 HTML 字节哈希完全相同。
- Tavily basic：两个缺失路径各请求两次，四次均返回 4131 字符、相同文本哈希、入门页内容，报告 URL 仍是所请求的缺失路径。正常页返回 12477 字符，首页返回 5459 字符；缺失路径与首页提取格式并不完全相同。
- 本地 read_page：按当前“取得非空提取文本”规则判 success，内容保留入门页；没有本地替换成其他页面的证据。

支持的因果范围：即使绕过 Tavily，本次访问链路也会在未知路径返回首页 HTML，因此现象不是必须由 Extract 单独造成。更准确的描述是“站点或其访问链路对这些路径返回 200 首页回退，抓取成功却不满足用户目标”。本机可能经过代理/CDN，未取得站点服务端日志，不能进一步锁定源站路由、CDN 或代理；也不能凭重复哈希断定 Tavily 缓存机制。

下一步若要设计目标匹配处理，应先确定如何识别预期标题/主题与通用回退内容的冲突，避免把所有 200 或非空正文认证为目标已核实。

证据：[HTTP 元数据、原始 provider 响应与 harness 结果](extract-match-20260910.json)，脚本：[Extract 探查](../../scripts/probe_extract_20260910.py)。

## 交互与输出证据

生成中取消及搜索观察见 [PTY 摘要](terminal-cancel-20260910.md)。独立目录中使用显式 --env-file 的两次真实问答和退出码见 [CLI 子进程记录](cli-entry-20260910.json)。该子进程 stdout 仅有 `已记住。` 和 `枫叶73`，欢迎/输入提示/轮次/计时在 stderr。

历史“真实生成中 Ctrl+C 未确认”现由一次有效 PTY 取消补足，但工具阻塞阶段取消、人工终端差异和连接释放测量继续保留为未测。CLI 的工程安装验证与真实研究回归均有证据；模型拒绝超长输出、选择错误版本、错误归因属于各自独立结果，不应算安装包装失败。
